name: C++ CI
"on":
  push:
    paths:
      - "src/**"
      - "tests/**"
      - "meson_options.txt"
      - "README.md"
      - ".clang-format"
      - ".clang-tidy"
      - ".github/workflows/cpp.yml"
      - "meson.build"
  workflow_dispatch:

jobs:
  ci:
    permissions:
      contents: read
      security-events: write
    uses: FyraLabs/actions/.github/workflows/cpp.yml@main
    with:
      container: ghcr.io/terrapkg/builder:44
      configure-command: meson -B build
      build-command: meson compile -C build
      test-command: meson test -C build --print-errorlogs
